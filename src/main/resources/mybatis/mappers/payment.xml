<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="payment">

<!-- 장바구니 리스트 가져오기 -->
	<select id="selectShoppingList" parameterType="int" resultType="com.javaex.vo.ShoppingCartVo">
	<![CDATA[
		 SELECT *
         FROM ShoppingCart
         WHERE userNo = #{userNo}
	]]>
	</select>
	
	<!-- History 테이블에 결제 정보 삽입 -->
    <insert id="insertPayment" parameterType="com.javaex.vo.PurchaseVo">
        INSERT INTO History (userNo, prodNo, shippingStatus, prodPrice, prodSize, purchaseDate)
        VALUES (#{userNo}, #{prodNo}, '준비중', #{prodPrice}, #{prodSize}, now())
    </insert>

    <!-- 장바구니 삭제 -->
    <delete id="deleteShoppingCart" parameterType="int">
        DELETE FROM ShoppingCart
        WHERE userNo = #{userNo}
    </delete>
	
</mapper>